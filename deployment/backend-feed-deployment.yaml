apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-feed
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend-feed
  template:
    metadata:
      labels:
        app: backend-feed
    spec:
      containers:
        - name: backend-feed
          image: duylkend/udagram-api-feed:1.0
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: env-configmap
            - configMapRef:
                name: env-secret